<!DOCTYPE html>
<html lang="en">
    <head>
        <title>{{ title }}</title>
        <meta name="viewport" content="width=device-width">
    </head>
    <body>
        <h1>{{ title }}</h1>
        <main>

            {{ content }}

        </main>
    </body>
</html>
